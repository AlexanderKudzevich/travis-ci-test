language: java

jdk:
  - oraclejdk8

jobs:
  include:
    - stage: verify
      script: mvn verify
    - stage: verify
      script: mvn verify
    - stage: test
      script: "curl http://www.google.com"